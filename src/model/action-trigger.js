/**
 * The Action Trigger is the function used to "fire" the Action.
 * The Action Trigger performs validation on the payload, and calls all of the
 * Action's call targets.
 *
 * @param {???} payload the data attached to the Action invocation
 */
export default function ActionTrigger(payload) {

};