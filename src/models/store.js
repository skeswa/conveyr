/**
 * TODO (Sandile): write documentation
 */
class Store {
    constructor() {
        // TODO (Sandile): write this
    }

    /**
     * TODO (Sandile): write documentation
     * @param  {[type]} thing [description]
     * @return {[type]}       [description]
     */
    static validate(thing) {
        return thing instanceof Store;
    }
}

export default Store;